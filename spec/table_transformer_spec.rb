require 'spec_helper'

describe TableTransformer do
  it 'has a version number' do
    expect(TableTransformer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
